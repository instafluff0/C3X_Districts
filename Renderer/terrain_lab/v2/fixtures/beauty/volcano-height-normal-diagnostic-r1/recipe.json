{
  "source": "Renderer/terrain_lab/v2/fixtures/beauty/shadow-receiver-r1/combinedvolcano/combined.hlsl",
  "replace": "float2 volcano_height = volcano_height_texture.Sample(\n                material_sampler, volcano_uv).rg;\n            material_normal = normalize(float3(\n                material_normal.x + (volcano_height.x * 2.0 - 1.0) * 0.34,\n                material_normal.y + (volcano_height.y * 2.0 - 1.0) * 0.34,\n                material_normal.z));",
  "with": "uint volcano_width,volcano_height;volcano_height_texture.GetDimensions(volcano_width,volcano_height);\n            float2 step_uv=1.0/float2(volcano_width,volcano_height);\n            float left=volcano_height_texture.Sample(material_sampler,volcano_uv-float2(step_uv.x,0)).r;\n            float right=volcano_height_texture.Sample(material_sampler,volcano_uv+float2(step_uv.x,0)).r;\n            float down=volcano_height_texture.Sample(material_sampler,volcano_uv-float2(0,step_uv.y)).r;\n            float up=volcano_height_texture.Sample(material_sampler,volcano_uv+float2(0,step_uv.y)).r;\n            float3 source_normal=normalize(float3(geometry_normal.xy+float2(left-right,down-up)*normal_strength*geometry_normal.z,geometry_normal.z));\n            material_normal=normalize(lerp(material_normal,source_normal,q4_volcano_coverage(input)));",
  "classification": "source_height_normal_adaptation"
}
