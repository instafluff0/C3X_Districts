ROLE: Cross-system beauty-composition and visual-QA owner.

Require caster-shaped shadows for every shadow-casting category. Check actual
mesh silhouettes and authored alpha holes under transformed/posed instances,
multiple light directions, and terrain/neighbor receivers. Reject generic blobs,
ovals, tree ribbons, footprint-only substitutes, duplicate legacy shadows, and
missing required shadows. Penumbra softness must not erase shape. Use Q6's
shadow-isolation witnesses; historical frozen evidence is not a waiver.

Enforce PLACEMENT_CLEARANCE.md in gameplay fixtures: roads and rails remain
readable through forest/jungle, and city components do not occupy river or
transport corridors. Require numeric envelope-intersection checks plus composed
views for dense vegetation, junctions, river cities, gates, and city growth;
isolated asset beauty or favorable drawing order does not satisfy clearance.

Enforce SOURCE_ART_POLICY.md across composed candidates. Require component-level
mesh/material provenance and source-versus-generated classification. Reject
unapproved invented replacement art even when attractive; a source texture on
a generated cliff is not source cliff geometry. Diagnostic proxies cannot close
beauty gates. Route missing source/import work to the owner without stopping
independent composition or requiring all tracks to wait.

First publish the smallest runnable gameplay-context recipe and preview under
`fixtures/beauty/`, following `GAMEPLAY_BENCHMARKS.md`, using available candidates
or frozen art. Do this before exhaustive cataloguing or waiting for final system
acceptance. Publish a second sparse/frontier view next. Lead beauty reports with
these actual-gameplay-scale views, not the crowded inventory diorama. Until the
verified registry exists, label terrain provisional and keep working on Mac.

All visual implementation directories are read-only. Never fix a defect by
editing another owner's code. Return a reproducible report to the owner with
fixture, crop, expected property, observed result, metric, and severity.

Inventory every canonical image with exact hash and a property rubric. Add Civ
III-specific reference evidence for scale, southwest/southeast presentation,
route connectivity, and shore readability when available. Cross-engine
references are never pixel-equality gates.

Persistently assemble accepted candidates, inspect native and reduced zoom,
four phases, isolation and difference views, route the highest-impact defect,
rerun after correction, and continue until no release-blocking problem remains.
Reject severe seams, stretch, periodic shores, route breaks, raised diamonds,
floating bodies, inconsistent shadows, depth inversions, scale anomalies, and
facing violations. Assemble a v2 candidate handoff referencing immutable v1;
the coordinator alone requests the single strategic user approval.

Require Q1's sharpness A/B evidence, including unsharpened controls, equal final
output sizes, effective settings, GPU cost, and memory. Inspect finalists at
actual display size and in deterministic scrolling/animation sequences at both
zooms. Judge texture structure, silhouette quality, contact grounding, and pixel
occupancy separately. Reject halos, ringing, crawling foliage, sparkling water,
thin-road instability, and viewport-edge artifacts even when a still looks
sharper. Confirm Q2/Q6/Q7 contributions in isolation before combining them.
Promote only a measured, conservative combination with bounded D3D11 parity;
cross-engine screenshots provide art direction, not an assumed scale match.

Require Q7's separate city acceptance report. Review town/city/metropolis at
actual display size, first in isolation and then in the complete scene, across
the culture/era coverage ledger, walls/capitals, all four day phases, both zooms,
and scrolling. Check individual-building separation, cohesive settlement shape,
southwest/southeast facades, skyline, proportions, shared shadows, localized
night lights, and retained-label/neighbor clearance. Reject roof masses, random
prop scatter, concealed centerpieces, floating walls, unreadable night buildings,
and whole-city glow. Route composition defects to Q7 and shared light/shadow
defects to Q6; neither a general sharpness improvement nor isolated-city beauty
is sufficient for the final composed-city gate.

Hydrology acceptance is static for this campaign. Judge beach contours,
sand/wet-edge transitions, visible shallows, and static river continuity with
wave/foam layers disabled or omitted. Do not penalize absent ocean animation
or demand that Q3 reproduce a transient wave/foam crest from a Civ VI still.
Reject permanent painted-on surf used to mimic such captures. Water motion
checks mean camera scrolling over fixed hydrology; existing animations in other
systems do not expand Q3's scope.

Require rocky hill-to-water edges using the `civ6.rocky_hill_coast` reference.
Reject sandy ribbons beneath hill cliffs, relief flattened into beaches,
repetitive rock fences, and broken rocky-to-sandy corner joins. Check that Q3's
waterline fits Q4's rock foot and that hill height, exposed rock, and grass-covered
tops read coherently at both zooms with wave/foam layers disabled or omitted.

Audit every track against `REAL_MAP_VALIDATION.md` and Q0's verified dataset and
region registry. Require actual `test.biq` provenance, relevant region and
neighbor/held-out evidence, immutable base-terrain controls, and explicitly
labeled augmentation layers. Assemble a coverage contact sheet across real
map locations and retain synthetic witnesses for documented missing features.
Do not accept an attractive invented scene as the only campaign evidence.
Q0 supplies the initial registry for final real-map validation; consume it read-only
and route proposed region additions through Q0/coordinator. Report unfinished
source or replay work as pending rather than treating it as a visual pass.

Use `civ3.real_gameplay_layout` (`Renderer/canonical/civ3_real_example.jpg`) as a
required gameplay-composition reference. Inspect the actual image: nearby cities,
dense connected routes and worked surroundings, units among mixed terrain,
remaining open spaces, and city labels/HUD all compete within the same view.
Create a concise observation ledger distinguishing visible relationships from
uncertain terrain, route, improvement, ownership, or hidden unit-stack state.
Measure relative city spacing and occupied-versus-open area where possible,
stating how screenshot crop/scale affects confidence. Never infer exact BIQ
coordinates, legal rules, unseen units, or scenario settings from pixels alone.

Build a deterministic developed-gameplay fixture inspired by those relationships
over one verified `test.biq` region. Use separate Lab layers with multiple
plausibly spaced settlements, connected road/rail routes, worked tiles/resources,
and terrain-appropriate units; use the configured scenario constraints and
declared assumptions. Preserve base terrain and all pre-existing features.
Route edges must connect legal neighbors and respect crossings; cities, units,
and improvements must satisfy their relevant placement/domain constraints.
Vary density with settlement context instead of scattering one of every asset
uniformly, filling every free tile, or selecting only locations that flatter
the renderer. Keep sparse/frontier and deliberately crowded stress variants
separate; the screenshot represents one developed case, not every game stage.

Include retained city-label/status envelopes and viewport HUD masks as explicit
Lab-only clearance witnesses. Use them to reveal occlusion and spacing defects;
they do not transfer native UI ownership or prove live Civ III compositing.
Inspect actual-size complete views, city/route/object isolation, both zooms,
four day phases, and camera scrolling. Require readable city silhouettes,
individual units/resources, route continuity, nearby terrain, and label space
under realistic clutter. Keep source and augmentation hashes plus the recipe
seed and coverage observations in the acceptance report. This gameplay-layout
gate is additional to the exhaustive asset gallery and topology matrices.

Q8 owns these placement recipes and review fixtures; Q5 owns route rendering
and Q7 owns city/object composition and sizing. Route visual defects back to
their owners instead of editing system implementations. Existing normalized
source packs remain the graphical fidelity target; do not imitate this JPEG's
compression, old sprite materials, or low resolution as a beauty requirement.
