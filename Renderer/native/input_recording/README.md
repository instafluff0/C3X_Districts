# Renderer input journal (development)

This is a separate input protocol from the v2/v3 composition/pixel journal.
`C3X_RENDERER_INPUT_RECORD_DIR` selects a **new** directory. Unset means no writer,
module pin, file activity or capture queue. Capture is not enabled in the installer.

Creating `stop.txt` in the active input directory requests a clean close. The
writer observes it while idle and admission closes between complete transactions;
the game and independent visuals keep running. `finished.json` and the verified
footer establish completion, not the stop file alone. The short diagnostic
launcher exposes this through Enter and stops its external collectors afterward.

The optional DLL recorder copies explicit API fields and CPU image inputs. It
records scene/world records, GPU and CPU unit actions, image commands, tactical
primitives, GPU and CPU camera calls, sampled visual clocks, visual offers/policy,
native lifetime checks, native CPU screens, presentation results, configuration,
world-callback registration and resets. It never records process pointers or C++
object layouts. Renderer-produced map and unit pixels are regenerated by replay.
Explicit image readbacks and CPU output hashes supply independent witnesses rather
than replacement images. CPU camera adoption also checks epochs and the complete
ordered display description. CPU unit fallbacks copy GDI inputs in canonical
orientation and compare the actual renderer's output afterward.

Native screen inputs use changed 8,192-word blocks with predecessor/result hashes.
An unchanged screen references its previous content; recording retains only block
fingerprints. CPU unit canvases separately track native changes and renderer writes.
Capture supports rectangular-clipped 16-bit RGB555/RGB565 and 32-bit DIBs with
ordinary GDI translation. Unsupported transforms, complex clips or formats stop
recording explicitly. The full native fixture also exercises the production composition owner and
image adapter, using copied metadata, private pixel leases, palette/sprite data,
font/DC state, lookup tables and navigation fields. Native object IDs survive both
lifetime notification and adapter destruction; only then are they retired.
Consumed external values live in a separate replay provider, so proxy IDs are
never dereferenced as game objects. Recording failure falls back to the original
native dependency without duplicating a borrowed pixel lease.

The writer has a 32 MiB queue charged by buffer capacity (including the in-flight
write), at most two 16 MiB producer buffers, 128 MiB segments and an 8 GiB disk cap.
CPU fallback capture adds one process-wide scratch DIB (at most approximately 10.8 MiB), bounded
hash/identity tables, and small stream/metadata allocations. Replay may hold a bounded 96 MiB native dependency cache (retired on image destruction), eight
CPU canvases and a native screen; its memory is separate from game recording.
Serial CPU callers share IDs and the scratch budget. Concurrent CPU producers,
section-backed DIBs and overlapping destination/background storage stop capture
instead of silently replaying different ownership. Reset and explicit finish
release the scratch even after recording has stopped.
Disk preflight requires the cap plus 64 MiB free; it does not reserve space.
Segment/event metadata and payloads are checksummed. Overflow, disk failure and
unsupported payloads stop capture. Sequence chaining and checksums reject missing
or corrupt segments. The 600-second gameplay timer begins at the first successful
GPU presentation and stops at a completed call boundary. The existing writer checks the deadline
while idle, so a quiet game needs no extra renderer callback to close capture.
Explicit finish requires
quiesced callers. A killed process leaves a prefix, never a certified complete session.

Wire values are little-endian. Segment headers carry version, ordinal, QPC
frequency and predecessor sequence; events carry kind/subtype, sequence, arrival
time, length and checksum. Call tokens pair input and result events; clock events
refer to their consuming call. A per-second/presentation index contains 64-bit
segment offsets. Seeking must rebuild the full prefix. The index alone is not a
checkpoint. Protocol 10 also records native owner calls and their explicit external
dependencies; protocols 9/10 are incompatible with older tools. Protocol 8 added an absolute QPC/UTC correlation bracket, owned exported
visual-clock queries and idle duration closure. It also includes CPU camera lifecycle, adopted-view witnesses, runtime
control changes, native screen deltas, presentation caller ownership and exact GPU
adoption sample/view witnesses. Older development protocols require
their matching frozen tools/DLL; they are not silently upgraded.

SHA-256 manifests pin the DLL and files observed at production load boundaries,
including optional missing assets. Recorded paths resolve through filesystem
handles, so temporary UNC drive mappings do not become dependencies. Replay checks the manifest before loading and
checks assets actually consumed. Environment controls are copied initially and
when changed at input boundaries. Trace/capture destinations remain local to the
new invocation. Local captures can contain resolved local paths and native UI
content; they remain ignored and must not be committed.

`BUILD.bat input-recording` builds portable protocol/storage controls.
`BUILD.bat input-replay` builds `replay_inputs.exe`. The replay tool requires
`--development DLL DIRECTORY`; it cannot claim qualified gameplay replay.
Optional `--frames NEW_DIRECTORY LAST_FRAME`, `--range NEW_DIRECTORY FIRST LAST`,
or `--seconds NEW_DIRECTORY START END` export the retained presentation surface
while executing the complete input prefix. Frame and second selection have both
been checked against full replay. The timeline includes GPU, native CPU
and ambient-only accepted presentations; `--timeline NEW_FILE` writes frame/time
identities. These are composed presentation sources, not physical scanout.
`inspect_inputs INPUT_DIRECTORY NEW_OUTPUT_DIRECTORY [--allow-prefix]` verifies
segments and emits a call/presentation timeline without a GPU. A torn final record
preserves the verified prefix; incomplete captures never become qualified sessions. Timing in this forensic mode
includes readiness controls and is not a performance result. `--allow-prefix`
permits a verified torn final record or explicitly incomplete capture; corruption
and missing interior segments still fail. The report retains `complete:false`.
`--compare-candidate` explicitly permits a different DLL while preserving asset,
input and output checks; reports disclose whether the binary matches the capture.
`--trace NEW_FILE` enables a separate production trace for diagnosing divergence.
`--watch` opens a visible window and paces calls against recorded input arrival.
Every accepted presentation is retained; a slow player reports lag instead of
skipping frames. Escape stops playback. Viewing is forensic and cannot be combined
with performance mode. For captured game sessions, double-click
`Renderer/PLAY_REPLAY.bat` on Windows and choose the session directory. It uses that
session's frozen DLL and replay executable, verifies their receipt hashes, and
requires the original local assets. Older pixel-only captures cannot use it.
`--realtime NEW_JSONL` instead paces external calls and enables the production
ambient scheduler, replacing recorded ambient offers. New frames follow wall time;
the player logs input lateness and independent presentation timestamps. Exact
pixel witnesses remain in forensic mode; real-time mode retains structural checks
and permits different time-dependent samples. See [Before/After playback](../../docs/realtime_replay_comparison.md)
for the sequential Windows launcher and remaining native-consumption constraints.
`--fingerprints NEW_FILE` reads each accepted retained display and stores its
extent/identity and 128-bit BGRA content fingerprint, without writing full BMPs.
This is replay-only oracle work, never capture overhead or a performance run.
`python -m Renderer.native.validate_input_replay --capture INPUT_DIRECTORY --dll DLL --out NEW_DIRECTORY`
freezes its DLL and tools, checks the complete capture and compares two fingerprint
runs. `--tools DIRECTORY` selects frozen tools for a historical protocol. Add
`--compare-candidate` only for an explicit changed-DLL comparison.

Run `python -m Renderer.native.test_input_replay --scene PATH` for production
capture, two replay controls, and missing-clock/asset/unit/reset plus altered
configuration/visibility/action/CPU-write mutations. The
protocol-8 short control passed at 640×480: 170 production calls, 15 visual clock
samples, all six composed presentations identical in two replays, frame/time
selection matching the full prefix, and explicit incomplete-prefix recovery.
All eight mutations reject with clean exit code 1 and the expected diagnostic.
The 6.61 MB capture used 262,496 peak queued bytes. It includes CPU caller-thread
changes; separate native controls check shared scratch lifetime, alias rejection
and concurrent capture stopping without changing the original draw result.
Local evidence:
`build/input-recording/contract-c9c51419be9847c78c29e189e17de865/receipt.json`.
These controls also cover stable filesystem identity after a VM command session's
temporary shared-drive mapping disappears. This is narrow proof, not gameplay
qualification.

**Not yet qualified:** the manifest deliberately declares zero complete-coverage
bits. The first 2240×1260 native fixture exposed queue overflow after 985 MB;
620 MB repeated identical native screens. Changed-block storage then produced a complete 107-second capture (623 MB,
13,119 calls, 520 accepted presentations, about 16 MiB peak writer queue). Full
replay exposed caller ownership and asynchronous rejection handling, both repaired.
A later reset mismatch exposed a production geometry-reuse defect: translated
geometry kept the preceding camera's off-screen contributor selection (1,511
instead of the new view's 1,403). Reuse now checks selection membership; compiled
world content remains reusable. The repaired full native fixture passed. Two
unchanged 2240×1260 replays then passed all recorded output witnesses and produced
519 byte-identical retained presentation images across 13,128 calls and 761 clock
samples. The capture lasted 106.522 seconds and stored 623,552,891 bytes. These are
repeatability and storage measurements, not game FPS. Local evidence is in
`build/input-recording/full-native-20260921-e/replay-receipt.json`.

Whole-world input also passed: the 5,000-tile Standard fixture captured 198 page
returns, rendered 100 distributed destinations, and passed six independent cold
pixel comparisons. Two replays passed all output witnesses and exported 100
byte-identical composed images. Its 69.428-second journal is 45.95 MB; local
evidence is `build/input-recording/world-native-20260921-a/replay-receipt.json`.

The current world-page guard also passed a fresh Standard capture and two
100-frame replays: 1,170 calls, 201 page returns, 207 clocks and 46.07 MB of
journal storage. It rejects changed map/viewer/visibility identity and topology,
and rejects pages issued before a newer publication. Valid pages retain the
publication's owned projection/time metadata. Portable tests verify rejection
leaves the cursor and publication intact, then a fresh retry succeeds. Evidence:
`build/input-recording/world-scope-native-20260921-a/receipt.json` and
`world-scope-replay-20260921-a/receipt.json`.
The three valid-checksum world mutations (`alter-world-range`,
`alter-world-scope`, `alter-world-topology`) each reject with exit code 1 and
`production replay return code differs: expected=1 actual=0`; receipts are in
that replay's `mutations-b/`. The offline mutator uses a separate bounded 128 MiB
queue for unpaced file transformation; live recording remains limited to 32 MiB.

The [coverage ledger](coverage.md) distinguishes capture-tool admission from
complete live-game qualification. The native boundary now replays the actual
composition/image/navigation/reset owners rather than their flattened GPU calls.
The fresh 2240×1260 fixture records 12,930 calls, 949 native-owner calls, 678 clock
samples and 426 presentation boundaries in 98.974 seconds (515.56 MB, 17.78 MB peak
writer queue). Two exact-DLL replays pass all output witnesses and match every
presentation fingerprint. Evidence: `native-bridge-native-20260921-d/` and
`native-bridge-replay-20260921-d/` beneath `build/input-recording/`.
The test also exposed and repaired recorder identity retirement between the two
native destroy notifications; the original game path was unchanged.

`python -m Renderer.native.measure_input_replay --capture DIRECTORY --dll DLL --out NEW_DIRECTORY`
runs separate production-boundary measurements. Add `--compare-candidate` for an
explicit changed DLL, `--reserve-mib N` for declared address-space capacity, and
`--runs N` for repetitions. This mode disables display fingerprint/export work
and times native production calls after reconstructing external inputs. Real
worker readiness is awaited at the consuming boundary; old work durations are
never slept. Earlier camera offers remain pending until the recorded consumption
point because the required native CPU input snapshot does not exist earlier.
Thus these are unpaced service-cost experiments, **not** a simulation of a faster
game caller, original overlap, input-to-display latency or live FPS. Memory samples
include replay storage; VA reservations do not reproduce heap fragmentation.
Changed external input requirements reject instead of synthesizing unseen data.
Forensic replay remains the independent correctness gate.

The one-command [game capture](../../docs/live_usage_logging.md) collects inputs
and correlated window evidence. Its admission receipt pins the tested DLL and
helpers; a mismatch fails before game launch. The launcher parser, shared-path
quoting and automatic collector stop pass against an owned test window (13 samples,
no game launch). The first real session is still needed to calibrate live fidelity
and co-resident pressure. Sampled window evidence never claims every physical frame.

`record_gpu_frame --input-soak-seconds 30` adds a short wall-clock endurance
segment to the actual native fixture; `600` runs the full duration. Combine with
`--record-inputs` for the capture arm, and omit it for the control arm. Both use
eight mixed units and Standard topology, cycling idle, unit actions and camera
changes with native HUD edits. Samples separate the external 50 ms pause from
production call time, and report virtual-address headroom and retained history.
Ambient opportunities are explicit in this fixture; it does not establish
automatic native-window scheduling or complete gameplay fidelity.


The first real 600-second native endurance arm passed at 2240×1260 with a
declared 512 MiB virtual-address reservation. Recording closed at its duration
boundary with 92,130 completed calls, 10,200 accepted presentations, no open
calls, 680,140,978 storage bytes and 17,773,968 peak queued bytes. The soak
produced 6,338 independent ambient frames, 3,958 native frames and 201 map
preparations. Its sampled minimum available VA was 675.7 MiB and peak retained
history was 28.0 MiB. The matching capture-off arm passed too. Two exact-DLL replays
passed all recorded output witnesses and matched all 10,200 display fingerprints
(2.52 MB total). These are fixture endurance/capacity and forensic-repeatability
measurements, not game FPS. Evidence:
`build/input-recording/soak-600-on-20260921-a/receipt.json`, `inspection-host/`,
`soak-600-off-20260921-a/receipt.json` and `soak-600-replay-20260921-a/receipt.json`.

`python -m Renderer.native.compare_input_soak` compares matched on/off receipts.
The first pair excludes the initial 180 seconds in both arms because helper
compilation overlapped the off arm's start. Over the remaining 420 seconds,
idle median/p95 was 21.591/28.864 ms with recording and 21.880/28.906 ms without;
camera-change median/p95 was 89.809/97.197 versus 88.991/96.331 ms. Other camera-phase
iterations retain approximately 260–270 ms p95 tails in both arms. The samples
exclude the fixture's external 50 ms pause. A single ordered pair with uncontrolled
VM variation does not establish a universal capture-overhead allowance. Raw data,
exclusions and limitations are in `soak-600-comparison-20260921-a.json`.

The optional `record_gpu_frame --window-witness-seconds N` runs an external
sampled-window observer against the **test process only**. It does not start a
game or change installation. Its work competes for GPU/CPU and is diagnostic,
not a benchmark. See [window evidence](../../tools/window_witness.md).
The combined 2240×1260 fixture passed: 119 sampled window images, clock alignment
within 0.044 ms, all native pixel checks, and two matching 970-frame input replays.
This verifies the diagnostic tools together; it does not establish complete
native gameplay replay or equal live timing/memory conditions.
