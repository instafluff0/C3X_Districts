# Renderer Packs

Generated packs live here. They are ignored by git because local prototype packs may reference or contain derived game assets.

Run:

```powershell
py ..\tools\asset_compiler\c3x_asset_compiler.py build-prototype
```

from this directory, or run the command from the repository root with the full script path shown in `Renderer/README.md`.
